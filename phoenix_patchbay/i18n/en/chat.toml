# English chat strings (Markdown-formatted, sent to Telegram/Matrix/API).

# -- Session management -------------------------------------------------------

[session]
error_header = "**Session Error**"
error_body = """\
[{model}] An error occurred.
Your session has been preserved -- send another message to retry.
Use /new to start a fresh session if the problem persists."""
error_cause = "**Cause:** {hint}"
error_detail = "**Detail:** `{detail}`"
error_auth = "Authentication failed. Please re-authenticate the CLI (e.g. `codex auth` or check your API key)."
error_rate = "Rate limit reached. Wait a moment and try again."
error_context = "Context length exceeded. Use /new to start a fresh session."

reset_header = "**Session Reset**"
reset_body = """\
Session reset for {provider} in this chat only.
Other provider sessions were preserved.
Send a message to continue."""

age_warning = "[Session is {age} old. Use /new for a fresh start.]"

# #84: Tool-only turns (e.g. silent memory updates) surface this neutral
# status line instead of a blank message the user would never see.
empty_turn = "_(Agent updated memory silently — send another message to continue.)_"

recovered = "_Previous session could not be restored. A new session was started automatically._"
recovery_failed = "Could not recover your session after a fresh start. Please try again or use /new to start over."
sigkill = "Execution was interrupted. Please send the same request again."

not_found = "Session '{name}' not found."
ended = "Session '{name}' has ended. Start a new one with /session."
still_running = "Session '{name}' is still processing. Wait or use /stop to cancel."

# -- Stop/interrupt ------------------------------------------------------------

[stop]
header = "**Agent Stopped**"
killed = "{provider} terminated. All queued messages discarded."
nothing = "Nothing running right now."

[interrupt]
done = "Interrupted {count} process(es)."
nothing = "No active processes."

[abort_all]
done = "Stopped {count} process(es) across all agents."
nothing = "No active processes found on any agent."

# -- Timeout -------------------------------------------------------------------

[timeout]
error_header = "**Timeout**"
error_body = """\
[{model}] CLI was terminated after {minutes} min.
Your session has been preserved -- send another message to continue where it left off.
Use /new to start a fresh session."""

result_header = "**Timeout**"
result_body = """\
The agent timed out after {elapsed}s (limit: {configured}s).
Try a shorter prompt, or ask in smaller steps."""

warning_minutes = "Timeout in {mins} min"
warning_seconds = "Timeout in {secs}s"
extended = "Timeout extended (+{secs}s, {remaining} left)"

generic = "Agent timed out. Please try again."

# -- Startup / lifecycle -------------------------------------------------------

[startup]
first_start_header = "**Bot Started**"
first_start_body = "First start — ready to go."
reboot_header = "**Bot Started**"
reboot_body = "System reboot detected — back online."

restart_default = "Restart completed."
restart_header = "**Restarting**"
restart_body = "Bot is shutting down and will be back shortly."

upgrade_complete = "**Upgrade complete** `{old}` -> `{new}`"

matrix_restart = "**Bot restarted** ({reason})"
matrix_update = "**Update available:** `{version}`\nUse `/upgrade` to update."
matrix_upgraded_restarting = "Upgraded {old} → {new}. Restarting..."

[recovery]
named_header = "**Auto-Recovery**"
named_body = "Resuming background session **{session}**\n`{preview}`"
interrupted_header = "**Interrupted Task**"
interrupted_body = "A task was interrupted during restart.\n`{preview}`\nPlease re-send to continue."

# -- Memory --------------------------------------------------------------------

[memory]
header = "**Main Memory**"
empty = "Empty. The agent will build memory as you interact."
empty_tip = "*Tip: Ask your agent to \"remember\" something to get started.*"
filled_tip = "*Tip: The agent reads and updates this automatically.*"

# -- Status --------------------------------------------------------------------

[status]
header = "**Status**"
no_session = "No active session."
model_line = "Model: {model}"
model_line_configured = "Model: {model} (configured: {configured})"
effort_line = "Effort: {effort}"
topic_line = "Topic: {topic}"
session_line = "Session: `{sid}`"
messages_line = "Messages: {count}"
tokens_line = "Tokens: {tokens}"
cost_line = "Cost: ${cost}"
bg_header = "Background tasks: {count} running"
auth_header = "Auth:"
account_line = "Claude account: {account}"
agents_header = "Agents:"

# -- Technical footer ----------------------------------------------------------

[footer]
model = "Model: {name}"
tokens = "Tokens: {total} (in: {input}, out: {output})"
cost = "Cost: ${cost}"
time = "Time: {secs}s"

# -- Upgrade -------------------------------------------------------------------

[upgrade]
dev_header = "**Running From Source**"
dev_body = "Self-upgrade is not available for development installs.\nUpdate with `git pull` in your project directory."
pypi_unreachable = "Could not reach PyPI to check for updates. Try again later."
up_to_date_header = "**Already Up to Date**"
up_to_date_body = "Installed: `{current}`\nLatest:    `{latest}`\n\nYou're running the latest version."
available_header = "**Update Available**"
available_body = "Installed: `{current}`\nNew:       `{latest}`\n\nUpgrade now?"
btn_changelog = "Changelog v{version}"
btn_yes = "Yes, upgrade now"
btn_not_now = "Not now"

# -- Diagnose ------------------------------------------------------------------

[diagnose]
header = "**System Diagnostics**"
version_line = "Version: `{version}`"
configured_line = "Configured: {provider} / {model}"
effective_line = "Effective runtime: {provider} / {model}"
log_header = "Recent logs (last 50 lines):"
no_log = "No log file found."
codex_cache_not_init = "Codex Model Cache: Observer not initialized"
codex_cache_not_loaded = "Codex Model Cache: Not loaded"
codex_cache_info = "Codex Model Cache:\n  Last updated: {updated}\n  Models cached: {count}\n  Default model: {default}"
health_header = "**Multi-Agent Health:**"

# -- Tasks ---------------------------------------------------------------------

[tasks]
too_many = "Too many background tasks ({max} max)"
internal_error = "Internal error (check logs)"





# -- Sessions selector ---------------------------------------------------------

[sessions]
header = "**Sessions**"
empty = "No active sessions."
start_hint = "Start one with `/session <prompt>`."
followup_hint = "Follow up: `@<name> <message>`"
active_count = "Active: {count}"
btn_end = "End {name}"
btn_refresh = "Refresh"
btn_end_all = "End All"
topics_header = "Topics:"
named_header = "Named:"
named_empty = "No active sessions.\n  Start one with `/session <prompt>`."

ended_all_one = "All {count} session(s) ended."
ended_all_none = "No active sessions."
ended_one = "Session '{name}' ended."
ended_not_found = "Session '{name}' not found."
unknown_action = "Unknown action."

# -- Model selector ------------------------------------------------------------

[model]
header = "**Model Selector**"
current = "Current: {model}"
current_with_effort = "Current: {model} ({effort})"
configured_default = "Configured default: {configured}"

switched = "**Model switched.**"
model_line = "Model: {model}"
model_change = "Model: {old} -> {new}"
provider_change = "Provider: {old} -> {new}"
reasoning_line = "Reasoning: {effort}"
effort_updated = "Reasoning effort set to {effort}."
already_running = "Already running {model}. No changes made."

resume_hint = """\
Resuming session `{sid}`.
You have already sent {count} {label} in this provider session.
Current model: `{model}`.
Use /new to start a fresh session."""
resume_msg_one = "message"
resume_msg_other = "messages"

no_auth = "No authenticated providers found.\nRun `claude auth`, `codex auth`, or authenticate in `gemini` to get started."
pick_provider = "Pick a provider:"
select_claude = "Select Claude model:"
select_gemini = "Select Gemini model:"
select_codex = "Select Codex model:"
select_antigravity = "Select Antigravity model:"
select_grok = "Select Grok Build model:"
no_codex = "No Codex models available."
no_gemini = "No Gemini models discovered from local Gemini CLI files."
thinking_level = "Thinking level for {model}:"
unknown_action = "Unknown action."
btn_back = "<< Back"
switched_while_busy = "**Model switched.** Current process still running — new model applies to the next message."

# -- Cron selector -------------------------------------------------------------

[cron]
header = "**Scheduled Tasks**"
empty = "No cron jobs configured."
empty_hint = "*Ask your agent: \"Run a backup check every day at 9am\"*"
job_line = "{number}. **{title}** ({status}){last_run}\n   `{schedule}`"
status_active = "active"
status_paused = "paused"
btn_disable = "{number}. Disable"
btn_enable = "{number}. Enable"
btn_prev = "<< Prev"
btn_refresh = "Refresh"
btn_next = "Next >>"
btn_all_on = "All ON"
btn_all_off = "All OFF"
info_active = "{active}/{total} active"
info_page = "page {current}/{total}"
all_enabled = "All cron jobs enabled."
all_disabled = "All cron jobs disabled."
already_enabled = "All cron jobs were already enabled."
already_disabled = "All cron jobs were already disabled."
toggled = "'{title}' {state}."
list_changed = "Cron list changed. Please try again."
unknown_action = "Unknown action. Refreshed cron list."

# -- Welcome (Telegram) -------------------------------------------------------

[welcome]
header = "**Welcome to Phoenix Patchbay{name}!**"
tagline = """\
Deploy from your pocket. Automate from your couch.
Claude Code, Codex & Gemini -- straight from Telegram."""
hint_model = "/model — switch models"
hint_info = "/info — docs & links"
hint_help = "/help — all commands"

no_auth = "No CLI authenticated yet. Run `claude auth`, `codex auth`, or authenticate in `gemini` to get started."
auth_line = "{providers} authenticated.\nModel: **{model}**"

btn_know = "Let's get to know each other!"
btn_system = "Check out the system!"
btn_who = "Who are you? Who am I?"

prompt_know = """\
Hey, I just set up Phoenix Patchbay and I want you to get to know me. \
Ask me everything you need to know so we can work well together -- \
my name, what I do, what I'm working on, how I like to communicate. \
Save what you learn to your memory."""
prompt_system = """\
Take a look around the system you're running on. \
Check the OS, installed tools, project folders, whatever you find interesting. \
Give me a quick summary of what you see."""
prompt_who = """\
Let's get started! Introduce yourself -- who are you, what can you do for me? \
Then ask me who I am and what I need help with."""

# -- Info ----------------------------------------------------------------------

[info]
header = "**Phoenix Patchbay**"
version = "Version: `{version}`"
telegram_description = "AI coding agents (Claude, Codex, Gemini) on Telegram.\nNamed sessions, persistent memory, cron jobs, webhooks, live streaming."
matrix_description = "AI coding agents (Claude, Codex, Gemini) on Matrix.\nNamed sessions, persistent memory, cron jobs, webhooks, live streaming."
slack_description = "AI coding agents (Claude, Codex, Gemini) on Slack.\nNamed sessions, persistent memory, cron jobs, webhooks, live streaming."

# -- Help (categories) --------------------------------------------------------

[help]
header = "**Command Reference**"
cat_daily = "Daily"
cat_automation = "Automation"
cat_multiagent = "Multi-Agent"
cat_browse = "Browse & Info"
cat_maintenance = "Maintenance"
footer = "Send any message to start working with your agent."
matrix_footer = "Use `!` or `/` prefix. Send any message to start."
slack_footer = "Use `help` or `/help`. Send any other message to start."

# -- Multi-agent ---------------------------------------------------------------

[agents]
header = "**Agents**"
empty = "No agents."
not_active = "Multi-agent mode is not active."
usage_stop = "Usage: /agent_stop <name>"
usage_start = "Usage: /agent_start <name>"
usage_restart = "Usage: /agent_restart <name>"
cannot_stop_main = "Cannot stop the main agent."
not_running = "Agent '{name}' is not running."
stopped = "Agent '{name}' stopped."

system_header = "**Multi-Agent System**"
telegram_explanation = """\
The multi-agent system lets you run additional bots as \
sub-agents — each with its own Telegram token, workspace, \
and user list. All agents share a single process and can \
communicate via the inter-agent bus."""
matrix_explanation = """\
The multi-agent system lets you run additional bots as \
sub-agents — each with its own workspace and user list. \
All agents share a single process and can communicate \
via the inter-agent bus."""
commands_header = "**Commands**"
setup_header = "**Setup**"
setup_instruction = "Ask your agent to create a new sub-agent or edit `agents.json` in your patchbay home directory."

# -- Errors (generic) ---------------------------------------------------------

[error]
generic = "Error: {detail}"
check_logs = "Error: check logs for details."

# -- Gemini warning ------------------------------------------------------------

[gemini]
missing_key = """\
Gemini is set to API-key auth mode, but `gemini_api_key` in \
`~/.phoenix-patchbay/config/config.json` is `"null"` or empty.
Why this is required: when patchbay calls Gemini CLI as an external process, \
Gemini CLI does not expose an internally entered API key to that caller.
Set a real API key in `gemini_api_key` and restart `patchbay`."""

# -- New session (handler) -----------------------------------------------------

# -- Transport-specific --------------------------------------------------------
[telegram]
group_rejected = "This bot is not authorized for this group."
channel_not_whitelisted = "This channel is not whitelisted, I am out."
where_header = "**Where**"
where_no_tracker = "Chat tracker not available."
where_empty = "No chat activity recorded yet."
leave_usage_header = "**Usage**"
leave_usage = "`/leave <group_id>`"
leave_invalid_id = "Invalid group ID."
leave_failed = "Failed to leave: {error}"
left_group = "Left group <code>{group_id}</code>."

[matrix]
room_rejected = "This bot is not authorized for this room."

# -- Session help (transport-specific) -----------------------------------------

[session_help]
header = "**Background Sessions**"
telegram_explanation = """\
Background sessions run tasks in parallel without blocking \
the main chat. Each session gets a unique name and runs \
independently — you can have multiple sessions active at once."""
usage_header = "**Usage**"
followup_header = "**Follow up**"
followup_line = "`@session-name <message>` — send a follow-up to a running session"
commands_header = "**Commands**"
telegram_sessions_cmd = "`/sessions` — view and manage all background sessions"
telegram_stop_cmd = "`/stop` — cancel the running session"
matrix_session_cmd = "`!session <prompt>` — start a background session"
matrix_sessions_cmd = "`!sessions` — view and manage all sessions"
matrix_stop_cmd = "`!stop` — cancel running session"
default_provider = "`/session <prompt>` — default provider"
claude_single = "`/session <prompt>` — runs on Claude"
claude_model = "`/session @opus <prompt>` — specific model"
codex_single = "`/session <prompt>` — runs on Codex"
grok_single = "`/session <prompt>` — runs on Grok Build"
gemini_single = "`/session <prompt>` — runs on Gemini"
gemini_model = "`/session @flash <prompt>` — specific model"
grok_model = "`/session @grok-4.5 <prompt>` — specific model"
claude_multi = "`/session @opus <prompt>` — Claude (opus)"
codex_multi = "`/session @codex <prompt>` — Codex"
grok_multi = "`/session @grok <prompt>` — Grok Build"
gemini_multi = "`/session @flash <prompt>` — Gemini (flash)"
explicit = "`/session @provider model <prompt>` — explicit"

# -- File browser --------------------------------------------------------------

[file_browser]
btn_cancel = "Cancel"
btn_no_upstream = "⚠︎ No upstream"
btn_pull = "⤓ Pull"
btn_pull_n = "⤓ Pull {count}"
btn_push_confirm = "⤒ Push {count} now"
btn_push_n = "⤒ Push {count}"
btn_push_none = "⤒ Nothing to push"
git_dirty = " · {count} uncommitted"
git_line = "`{branch}` · {ahead} ahead · {behind} behind (last fetch){dirty}"
git_no_upstream = "`{branch}` has no upstream branch."
pull_failed = "⚠️ Pull failed."
pull_ok = "✅ Pulled."
push_confirm = "Push {count} commit(s) from `{branch}`?"
push_failed = "⚠️ Push failed."
push_nothing = "Nothing to push — the branch matches its upstream."
push_ok = "✅ Pushed."
pick_root = "Choose a location:"
no_roots = "No browsable directories configured."
tap_hint = "Tap a folder to open it."
btn_home = "🏠 Home"
btn_zip = "🗜 Whole folder (zip)"
too_large = "`{name}` is {mb} MB — past Telegram's 50 MB limit for bots. Ask the agent to split or summarise it."
zip_too_large = "That folder is too large to send as a zip. Open a subfolder and try again."
send_failed = "Could not send `{name}`."
header = "**File Browser**"
empty = "(empty)"
btn_back = "◀︎ Back"

# Matrix variant
matrix_header = "**Workspace Files**"
matrix_hint = "_Use `!showfiles <dir>` to list a specific directory._"
matrix_dir_not_found = "Directory `{subdir}` not found."
matrix_permission_denied = "(permission denied)"
matrix_file_count = "{count} file(s)"
matrix_no_dirs = "(no workspace directories found)"

# -- Upgrade handler (Telegram) ------------------------------------------------
btn_download = "⬇️ Download"
btn_download_file = "📄 A single file"
download_menu = "Download from `{dir}`. What do you want?"
pick_file = "Tap a file in `{dir}` to have it sent to you."
no_files = "There are no files in this folder."
file_list_truncated = "Showing the first {count} files."
btn_bind = "📌 Use this folder"
btn_bind_confirm = "📌 Rebind"
bind_confirm = "This conversation already works in `{current}`. Move it to `{new}`?"
bound = "📌 This conversation now works in `{dir}`."
btn_path = "📋 Path"
path_dir = "Folder path — tap to copy:"
path_file = "File path — tap to copy:"
path_pick = "Or pick a file for its own path:"

[upgrade_handler]
btn_changelog = "Changelog v{version}"
btn_upgrade_now = "Upgrade now"
btn_later = "Later"
skipped = "Upgrade skipped."
already_in_progress = "Upgrade already in progress. Please wait."
in_progress = "Upgrading to {version}..."
verification_failed = "Upgrade could not verify a new installed version (still {version}) after automatic retry.{details}"
restarting = "Bot is restarting..."
no_changelog = "No changelog found for v{version}."
changelog_header = "**Changelog v{version}**"

# -- Multi-agent notifications -------------------------------------------------

[multiagent]
async_task_received = "📥 **Async task received** from `{sender}`\nSession: `{session}` · Task ID: `{task_id}`\n\n_{preview}_"

[effort]
select = "Select reasoning effort for {model}:"
unsupported = "Reasoning effort is not configurable for the `{provider}` provider."

[account]
default_label = "default"
active_line = "**Claude account:** {account}"
none_configured = "No Claude accounts configured. Add a `claude_accounts` map to config.json, e.g. `{\"work\": \"~/.claude-work\"}`."
select = "Select the credential store to authenticate with:"
switched = "Switched to Claude account **{account}**. It applies from the next message — the current session stays resumable."
switched_docker_warning = "Switched to Claude account **{account}**, but Docker sandbox mode is enabled and does not map the credential store into the container."
unknown = "Unknown Claude account `{account}`. Configured: {known}"

[skills]
builtin_note = "_Claude's own built-in skills are not listed here — they live inside the binary and load automatically._"
header = "**Skills** — {total} available across {groups} groups"
pick_group = "Choose a group:"
group_header = "**{group}** — {count} skills"
tap_hint = "Tap a skill to copy its command, then paste and add your request."
personal_label = "personal"
btn_back = "◀︎ Groups"
btn_copy = "📋 Copy /{name}"
none_found = "No skills found. Check that plugins are installed and enabled."
not_found = "No skill named `{name}`. Use /skills to browse."
no_description = "(no description)"
detail_header = "**{name}** — {group}"
slash_only_note = "🔒 Slash-only: this skill must be invoked explicitly; the model cannot load it on its own."

[persona]
header = "**Persona**"
ask_header = "**Which persona should handle this?**"
none_defined = "No personas are defined. Add agent files to your Claude Code config to use this."
default_label = "Default"
selected = "Persona: **{persona}**"
switched_notice = "Persona active: **{new}**. This conversation previously ran as {old}; earlier turns in the history were produced under that persona and do not describe the current one."
active_notice = "Persona active: **{persona}**. This statement is current for this turn; follow this persona's remit and working style."

[upload]
btn_upload = "⬆️ Upload here"
btn_files = "📄 Send files"
btn_folder = "📦 Send a folder (.zip)"
btn_confirm = "✅ Move {count}"
btn_cancel = "✖ Cancel"
menu = "Upload into `{dir}`. What are you sending?"
await_files = "Send one or more files now. Nothing is written to `{dir}` until you confirm."
await_folder = "Send one .zip file. Its contents are listed for confirmation before anything is written to `{dir}`."
staging_header = "📥 Staged for `{dir}`"
overwrite_marker = "⚠️ replaces"
overwrite_note = "⚠️ {count} existing file(s) will be replaced."
more = "…and {count} more"
nothing = "Nothing staged yet."
moved = "✅ Moved {count} file(s) into `{dir}`."
cancelled = "Upload cancelled — nothing was written."
landed = "📎 Saved to `{dir}`."
download_failed = "⚠️ Could not receive that file."
expect_zip = "⚠️ `{name}` is not a .zip. Send an archive, or go Back and choose Send files."
zip_busy = "⚠️ Already reviewing `{name}` — confirm or cancel that one first."
not_a_zip = "⚠️ That file is not a readable .zip."
zip_unsafe = "⚠️ Refused: the archive contains entries that would write outside the target folder."
zip_too_big = "⚠️ Refused: the archive unpacks to more than {mb} MB."
zip_too_many = "⚠️ Refused: the archive holds more than {count} files."
zip_empty = "⚠️ That archive contains no files."

[folder]
header = "Folder for this conversation:"
ask_header = "This conversation has no folder yet. Choose where the agent should work:"
none_defined = "No project folders are configured, so the shared workspace is the only place work can happen."
shared_label = "🗂 Shared workspace"
selected = "📌 Working in {dir}."
general_locked = "**General has no folder.** It is this group's shared area: it always works in the shared workspace and cannot be bound to a project directory. Open the project's own topic and choose the folder there."

[topics]
consult_name = "Consult"
general_title = "General"
general_scope = "This is the shared area. Agents here always work in the shared workspace: General cannot be bound to a project folder, so nothing typed here can reach a project by accident."
general_care = "Project work belongs in that project's own topic, where the folder is bound once and stays put."
consult_title = "Consult"
consult_purpose = "For questions, thinking and second opinions — not for project work."
consult_isolation = "Agents here are instructed not to read or write anything outside this topic's folder. That is a rule they follow, not a wall they cannot cross, so do not paste anything here you would mind being read."
consult_wipe = "This topic and everything in it, including its files and its history, is deleted automatically. Nothing here is durable."
consult_schedule = "Schedule: {schedule}"
consult_no_wipe = "Automatic clearing is switched off for this topic, so anything here stays until someone removes it."

[consult]
header = "**Consult wipe**"
explains = "Everything in the Consult topic — its files, its messages and its history — is deleted on this schedule."
schedule_hourly = "Every hour"
schedule_6h = "Every 6 hours"
schedule_daily = "Daily at {hour}:00"
schedule_weekly = "Weekly, Monday {hour}:00"
schedule_off = "Never"

[edits]
btn_manage = "🛠 Manage"
btn_rename = "✏️ Rename"
btn_newdir = "📁 New folder"
btn_delete = "🗑 Delete"
btn_apply = "✅ Apply"
btn_delete_continue = "Continue"
btn_delete_now = "🗑 Delete permanently"
manage = "Manage `{dir}`. These change files on disk."
ask_rename = "Send the new name for `{name}`."
ask_newdir = "Send a name for the new folder inside `{dir}`."
confirm_rename = "Rename `{old}` to `{new}` in `{dir}`?"
confirm_newdir = "Create the folder `{new}` inside `{dir}`?"
delete_first = "⚠️ Delete `{name}` — {count} file(s), {size}. This cannot be undone."
delete_second = "⚠️ Last chance. `{name}` and its {count} file(s) will be removed from `{dir}` permanently."
delete_large = "That is {count} files. Nobody has read that list — be sure."
renamed = "✏️ Renamed `{old}` to `{new}`."
created = "📁 Created `{name}`."
deleted = "🗑 Deleted `{name}`."
exists = "⚠️ `{name}` already exists here."
failed = "⚠️ Could not change `{name}`."
gone = "⚠️ That is no longer there."
refuse_root = "⚠️ That is a configured project root. Change it in the config, not here."
refuse_repo = "⚠️ That is a git repository. Untracked files in it exist nowhere else, so this will not delete it."
name_empty = "⚠️ A name is required."
name_illegal = "⚠️ A name cannot contain a slash or a backslash."
name_traversal = "⚠️ That name is not allowed."
name_too_long = "⚠️ That name is too long."
btn_move = "✂️ Move"
btn_copy = "⧉ Copy"
btn_paste_move = "📥 Move {name} here"
btn_paste_copy = "📥 Copy {name} here"
btn_clip_cancel = "Cancel"
btn_paste_now = "✅ Paste here"
marked_move = "✂️ `{name}` is marked to move. Open the destination folder and tap Manage."
marked_copy = "⧉ `{name}` is marked to copy. Open the destination folder and tap Manage."
clip_cleared = "Nothing is marked any more."
paste_confirm = "Copy `{name}` into `{dir}` — {count} file(s), {size}?"
paste_confirm_move = "Move `{name}` into `{dir}` — {count} file(s), {size}?"
moved = "✂️ Moved `{name}`."
copied = "⧉ Copied `{name}`."
paste_exists = "⚠️ Something with that name is already there. Nothing is ever overwritten — rename one of them first."
paste_into_self = "⚠️ A folder cannot be pasted inside itself."
paste_same_dir = "⚠️ It is already in that folder."
paste_no_dir = "⚠️ Choose a folder to paste into."
paste_outside = "⚠️ That destination is outside the browsable folders."

[menu]
header = "**Menu**"
hint = "Pick an action, or ✕ to close."
btn_close = "✕ Close"
btn_back = "◀︎ Menu"
panel_ready = "Menu panel installed. Tap the grid beside the message box to reach it."
item_files = "📁 Files"
item_folder = "📌 Folder"
item_persona = "👤 Persona"
item_model = "🧠 Model"
item_account = "🔑 Account"
item_skills = "🧩 Skills"
item_compact = "🗜 Compact"
item_clear = "🧹 Clear & New"
item_handoff = "📋 Handoff"
item_status = "📊 Status"
item_consult = "🧭 Consult"
item_help = "❓ Help"

[handoff]
nothing_to_compact = "Nothing to compact — this conversation has no active session yet. Send a message first."
compact_no_handoff = "⚠️ Compact stopped: no handoff was written, so there would be nothing to carry into the new session. The session is unchanged."
compacting = "🗜 Compacting — consolidating the handoff, then starting a fresh session. This takes a moment."
clearing = "🧹 Clearing — consolidating and archiving the handoff, then starting fresh. This takes a moment."
ready_now = "✅ The workspace is ready now. Send your message again."
folder_missing = "⚠️ Handoff cannot be written: `{detail}` does not exist. Nothing was sent to the agent."
not_writable = "⚠️ Handoff cannot be written: `{detail}` is not writable. Nothing was sent to the agent."
exclude_unwritable = "⚠️ Handoff cannot be protected from git: `{detail}`. Fix it and tap Retry — nothing was sent to the agent."
dir_uncreatable = "⚠️ Handoff directory cannot be created: `{detail}`. Nothing was sent to the agent."
btn_retry = "🔄 Retry"
btn_compact = "🗜 Compact"
btn_clear = "🧹 Clear & New"
btn_handoff = "📋 Handoff"
btn_archived = "📚 Archived"
none_yet = "No handoff yet for this conversation. One is written as work happens."
current = "**Handoff** — {size}, updated {when}."
archived_header = "**Archived handoffs** for this conversation:"
archived_none = "No archived handoffs for this conversation yet."
compacted = "🗜 Compacted. The handoff was consolidated and carried into a fresh session."
cleared = "🧹 Cleared. The handoff was archived and a fresh session started."
clear_confirm = "This ends the current task: the handoff is archived and the session starts fresh. The archive stays available under Archived."
btn_clear_confirm = "🧹 Archive and start fresh"
archive_failed = "⚠️ The handoff could not be archived, so nothing was cleared. The session is unchanged."

[turn]
btn_stop = "⏹ Stop"
stopped = "Stopped. Anything queued runs next."
